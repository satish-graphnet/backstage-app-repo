# Documents for python-app

This application has two endpoints:
- `/api/access-token/v1/info`
- `/api/access-token/v1/healthz`
- `/api/access-token/v1/get-token`

Here you could expand on what each of these endpoints do.

# How to access the app?

You can access the app by accessing this URL: `{{base-url}}/api/access-token/v1/healthz` 